name: ci

# Provisioned skeleton — extend with the build/test commands appropriate
# to your stack. The base path for this app is `/alex/dfe-digital-standards-copilot`.

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Confirm checkout
        run: |
          echo "Project: DfE Digital Standards - Copilot"
          echo "URL path: /alex/dfe-digital-standards-copilot"
          ls -la
