# .env.example — provisioned values copied here by the AI Provisioning Platform.
# Real .env (gitignored) should mirror this. AAD-only — there are no
# passwords to store. Auth flows through DefaultAzureCredential.

AIP_PG_HOST=pg-dfe-digital-standards-copilot-l7qmpra3xh3ga.postgres.database.azure.com
AIP_PG_DATABASE=app
# Auth: Microsoft Entra ID only. Get a token with:
#   az account get-access-token --resource-type oss-rdbms --query accessToken -o tsv
# Use it as the password for the pg driver. The token is short-lived (~1h);
# the @azure/identity SDK refreshes automatically.
